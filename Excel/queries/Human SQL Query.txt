SELECT 
    id,
    title,
    body,
    state,
    created_at,
    merged_at,
    html_url
FROM human_pr_human_pull_request
WHERE 
    lower(title) LIKE '%revert%'
    OR lower(title) LIKE '%rollback%'
    OR lower(title) LIKE '%roll back%'
ORDER BY created_at;
